import 'dart:ui';

abstract class AppColors {
  // Primary Palette
  static const Color primaryLine = Color(0xFF366277);
  static const Color primaryBackground = Color.fromARGB(255, 235, 234, 229);

  // Variant / Accent Palette
  static const Color secondaryLine = Color(0xFF2C617D);
  static const Color secondaryBackground = Color(0xFFFCF7E4);
}